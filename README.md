# 艾希模拟器
## before run
```bash
$ npm i
```
## run on windows
```bash
$ cls && node index.js
```

## run on linux
```bash
$ clear && node index.js
```

## run in quiet mode (only show report after simulating)
```bash
$ node index.js -q
```